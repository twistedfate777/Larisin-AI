services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    env_file: ./server/.env
    volumes:
      - ./server/data:/app/data

  client:
    build: ./client
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - server
